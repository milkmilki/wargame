# Wargame 作战系统重构任务书

项目地址：<https://github.com/milkmilki/wargame>

> **实施状态（2026-08-03）**：item 1～17、五项机制复查缺口及上一轮 4 个 P1 均已闭环。
> 验收：`run_tests.sh` 763/0、万场统计 `STATISTICS_PASS`、真实地图 4 seed × 1095 天健康、
> strict-mirror 3650 天逐日无破裂（优势分 0.0）。实现与风险闭环见
> [COMBAT_REFACTOR_CHANGES.md](COMBAT_REFACTOR_CHANGES.md)。

## 任务目标

重构当前作战系统，修复会导致战果偏置、数值套利、空间失真、围城异常和战斗体验单一的问题。

本次修改应优先保证：

1. 战斗结果不依赖军队 ID、创建顺序、遍历顺序或 A/B 阵营位置；
2. 士气真实影响部队战斗力、持续作战能力和溃败；
3. 增援必须实际抵达战场后才能参战；
4. 伤亡分配严格守恒，不能通过拆分军队套利；
5. 围城中的驻军、城防和城市属性使用清晰、统一的量纲；
6. 地形、补给、随机性和多方参战规则具有稳定、可解释的行为；
7. 所有关键规则都有自动化测试覆盖。

---

# 一、最高优先级：修复战斗正确性

## 1. 修复 A 方固定获胜偏置

### 当前问题

对称战斗中，双方可能共用同一个战斗骰，因此双方伤亡和士气变化可能完全同步。

当双方在同一轮同时满足失败条件时，例如：

- 同时兵力归零；
- 同时士气归零；
- 同时发生溃败；

当前逻辑可能固定将 A 方判为胜者。

如果 A/B 方的分配又受到军队 ID、创建顺序或遍历顺序影响，那么完全相同的战斗会产生隐藏偏置。

### 修改要求

- 战斗胜负不得由 A/B 位置、军队 ID、创建时间或数组遍历顺序决定。
- 明确定义双方同时失败时的结果。
- 推荐方案：
  - 双方同时撤退；
  - 判定为平局并脱离战斗；
  - 根据剩余有效兵力、剩余士气和伤亡比例等对称指标判定。
- 禁止使用“默认 A 方获胜”作为兜底逻辑。
- 若必须随机裁决，随机输入必须与阵营顺序无关，并支持固定种子复现。

### 验收标准

- 交换 A/B 方后，完全相同的战斗得到等价结果。
- 改变军队创建顺序或 ID 后，战果不变。
- 双方同时崩溃时，不得固定由 A 方获胜。
- 相同随机种子下，战斗结果可复现。

---

## 2. 重构士气与溃败逻辑

### 当前问题

当前一侧士气可能按兵力加权平均计算，而溃败阈值接近或等于 `0`。

这会造成：

- 只要还有少量部队士气大于零，整侧就可能继续作战；
- 士气为零的军队仍贡献完整攻击力；
- 一支很小的援军可以让大量零士气部队继续作战；
- 将援军拆成多支依次加入，可能重复获得士气奖励。

士气因此更像第二条生命值，而不是组织度或战斗意志。

### 修改要求

#### 单支军队士气

士气必须影响有效战斗力。

可参考：

```text
combat_efficiency = minimum_efficiency
                    + (1.0 - minimum_efficiency) * morale
```

建议：

```text
minimum_efficiency = 0.2
```

还可以增加以下状态：

```text
morale < 0.20  -> 战斗效率明显下降
morale < 0.10  -> 尝试退出前线
morale < 0.05  -> 强制溃退
morale == 0.00 -> 不得继续正常攻击
```

具体阈值应集中配置，不要散落在代码中。

#### 整体战线士气

- 整侧溃败阈值不应严格等于 `0`。
- 建议先使用可配置值：

```text
side_rout_threshold = 0.15
```

- 整体士气应考虑：
  - 各军队当前士气；
  - 当前有效兵力；
  - 已溃退或失去组织的兵力；
  - 近期伤亡；
  - 预备队情况；
  - 是否被包围或断补给。

#### 援军士气

- 援军加入不得按“每支军队一次”重复增加整体士气。
- 士气提升应按新增有效兵力占当前总兵力的比例计算。
- 相同总兵力的一支援军和多支援军，提供的总士气效果应基本一致。
- 建议设置单日或单场战斗的增援士气恢复上限。

### 验收标准

- 零士气军队不能继续输出完整伤害。
- 大量零士气军队不会因极小援军加入而恢复完整战斗能力。
- 同一批援军拆成多支后，不能获得额外士气收益。
- 士气极低的一方会在兵力完全归零前合理溃败。
- 士气计算不存在 NaN、负数或大于 1 的异常值。

---

## 3. 修复伤亡分配不守恒

### 当前问题

总伤亡分配到多支军队时，如果对每支军队分别四舍五入，可能出现：

- 所有分配值都变成零；
- 分配后的总伤亡高于原始总伤亡；
- 军队拆分数量影响实际伤亡；
- 小部队组合获得隐性减伤。

### 修改要求

使用严格守恒的整数分配算法，推荐“最大余数法”。

处理流程：

1. 计算每支军队的精确伤亡份额；
2. 先分配每个份额的整数部分；
3. 计算尚未分配的剩余伤亡；
4. 按小数余数从大到小补发；
5. 单支军队伤亡不得超过当前兵力；
6. 某支军队达到兵力上限后，将剩余伤亡继续分配给其他军队；
7. 最终分配总和必须等于可实际承受的总伤亡。

建议将该逻辑提取为独立纯函数，便于单元测试。

### 验收标准

```text
sum(actual_casualties) == min(expected_total_casualties, total_alive_strength)
```

并且对每支军队：

```text
0 <= army_casualties <= army_strength
```

将一支军队拆成多支后，在其他条件相同的情况下，总伤亡应保持一致或只存在可解释的极小差异。

---

# 二、恢复战役空间和机动意义

## 4. 增援必须实际抵达战场

### 当前问题

同一路段上处于移动状态的军队，可能不检查实际距离便立即加入战斗。

这会造成：

- 道路另一端的部队瞬间参战；
- 尚未到达的部队立即被冻结；
- 尚未抵达的援军立即贡献攻击力；
- 尚未抵达的援军立即提供士气；
- 快速击破和阻援失去意义。

### 修改要求

为每场战斗定义明确的接触点或战场位置。

军队只有满足以下条件之一才能加入战斗：

- 已处于战场允许半径内；
- 已移动到接触点；
- 增援 ETA 已结束。

建议：

```text
reinforcement_eta = distance_to_battle / effective_movement_speed
```

在抵达之前，军队应处于“前往战场”状态：

- 不贡献攻击力；
- 不承受该战场伤亡；
- 不提供战斗士气；
- 不占用前线宽度；
- 可以被拦截、撤回或改道；
- 战斗提前结束时自动取消增援。

### 验收标准

- 同一路段但距离较远的部队不能立即参战。
- 部队只有实际抵达后才贡献战斗力。
- 战斗在援军抵达前结束时，援军不计入参战部队。
- 道路方向或节点顺序变化不会影响距离计算正确性。
- 增援 ETA 在保存和读档后保持一致。

---

## 5. 增加正面宽度和预备队

### 当前问题

所有参战兵力可能同时线性贡献攻击力，导致战斗退化为纯粹堆兵。

狭窄道路、山口、平原和城市区域之间缺少有效区别。

### 修改要求

增加可配置的战场正面宽度：

```text
frontage_capacity
```

正面宽度可以由以下因素决定：

- 道路类型和道路宽度；
- 地形；
- 城市战、野战或攻城战；
- 工事；
- 将领、科技或阵型修正。

超过正面宽度的兵力进入预备队。

#### 前线部队

- 提供完整或主要攻击力；
- 承受主要伤亡；
- 占用 frontage。

#### 预备队

- 不提供完整攻击力；
- 可提供有限士气稳定；
- 当前线单位损失、溃退或退出后补入；
- 补入可以存在时间延迟；
- 不应因拆分成更多军队而获得额外优势。

### 验收标准

- 超过正面宽度后，继续增加兵力的边际收益明显降低。
- 狭窄地形中，大军不能全部同时输出。
- 前线单位损失后，预备队能够正常补充。
- 拆分军队不会增加总 frontage。
- 同等兵力和属性下，平原战比山口战允许更多兵力同时展开。

---

# 三、围城系统重构

## 6. 分离驻军、城防和城市属性

### 当前问题

围城逻辑可能混用以下数值：

- 驻军人数；
- 城市 defense 属性；
- 城墙或工事强度；
- 围城所需兵力参考值。

这些变量量纲不同，不能直接放入同一套倍数公式。

这会造成：

- 有驻军和无驻军时，围城需求出现数量级跳变；
- 驻军已经被击溃，但仍以原始驻军人数决定围城门槛；
- 城市几十点 defense 被错误当作几十名士兵；
- 小股部队可以轻易围困空城，大军却无法围困驻军已败的城市。

### 修改要求

明确拆分字段：

```text
garrison_strength
fort_strength
city_defense_modifier
siege_required_manpower
```

建议含义：

- `garrison_strength`：实际驻军人数，用于守城战和突围。
- `fort_strength`：城墙、工事和要塞的结构强度。
- `city_defense_modifier`：守军战斗倍率或围城难度倍率。
- `siege_required_manpower`：执行有效封锁所需的最低兵力，由城市规模和城防推导。

禁止：

- 直接用驻军人数替代城防强度；
- 直接用 defense 点数替代人数；
- 在没有单位说明的情况下，将不同量纲变量相乘或比较。

### 验收标准

- 有无驻军时，围城所需兵力不会出现无解释的数量级跳变。
- 驻军被击败后，城防仍可存在，但来自 `fort_strength`。
- 所有围城公式中的变量单位都有注释或文档说明。
- 调整驻军人数不会直接改变城墙结构强度。

---

## 7. 去掉“五倍兵力”硬门槛

### 当前问题

攻击方兵力略低于固定倍数时可能直接撤退，达到门槛后才开始产生围城进度。

这会造成：

- 赢下守城战后立刻撤离；
- 军队反复攻城、撤退、再攻击；
- 兵力只差一点，却从完全无效跳变为正常推进；
- 玩家难以理解围城行为。

### 修改要求

将硬门槛改为连续曲线。

建议结构：

```text
manpower_ratio = attacker_effective_strength / siege_required_manpower
```

根据比例决定围城效率：

```text
ratio < 0.5  -> 无法完全封锁，进度可能缓慢倒退
0.5 ~ 1.0    -> 部分封锁，进度极慢
1.0 ~ 2.0    -> 正常围城
2.0 ~ 4.0    -> 较高效率
> 4.0        -> 收益递减，不无限线性增长
```

兵力不足时，不应无条件自动撤退。可允许：

- 保持观察；
- 部分封锁；
- 等待援军；
- 主动撤退；
- 因补给不足或敌军威胁而撤退。

### 验收标准

- 围城效率随兵力比连续变化，不存在突然跳变。
- 赢下守城战后不会因为低于单一倍数门槛而立即自动离开。
- 极大兵力不会产生无限线性加速。
- 围城状态不会在攻击和撤退之间无限循环。

---

# 四、其他影响体验的问题

## 8. 重构战斗随机性

### 当前问题

如果双方完全共用一个战斗骰，随机数主要改变“当天战斗有多激烈”，却很少改变双方的相对表现。

这会造成：

- 对称战斗长期保持严格对称；
- 随机性只能加快或减慢战斗；
- 很难表现战术失误、临场发挥和局部逆转；
- 同时崩溃概率增加，并进一步触发 A 方偏置。

### 修改要求

将随机性拆为两部分。

#### 共享战场随机因素

双方共同受到影响，例如：

- 天气；
- 能见度；
- 战斗激烈程度；
- 道路或地形状况。

#### 独立战术随机因素

双方分别计算，例如：

- 指挥发挥；
- 战术执行；
- 临时失误；
- 局部突破。

推荐：

```text
shared_modifier = random_in_range(0.90, 1.10)
side_modifier   = random_in_range(0.95, 1.05)
```

具体区间应集中配置，并保持随机幅度适中，避免完全覆盖兵力和属性差异。

### 验收标准

- 对称战斗可以出现小幅非对称结果。
- 长期统计下，A/B 方胜率接近 50%。
- 优势方仍保持统计优势，不会被过大随机性完全淹没。
- 固定随机种子可以稳定复现战斗过程。

---

## 9. 平滑地形修正，去掉数值断崖

### 当前问题

部分地形或道路危险度可能在某个阈值处突然从平滑公式跳到极低倍率。

例如危险度仅增加极小值，攻击效率却可能瞬间下降超过一半。

这会造成：

- 地图生成中的微小差异产生巨大结果；
- 浮点误差触发完全不同的规则；
- 玩家难以理解相邻道路为何表现悬殊；
- 平衡调整困难。

### 修改要求

二选一：

#### 方案 A：连续曲线

使用连续且单调的函数计算地形修正，避免阈值跳变。

#### 方案 B：明确的离散地形类型

若确实需要极端狭窄区域，定义明确类型：

```text
PLAIN
HILLS
MOUNTAIN
CHOKEPOINT
RIVER_CROSSING
URBAN
```

每种类型使用清晰配置，而不是依赖某个浮点阈值隐式触发。

### 验收标准

- 地形参数的小幅变化只产生小幅战斗结果变化。
- 极端地形必须由明确类型或标记触发。
- 所有地形倍率集中配置。
- 不存在因浮点值跨过 0.001 而导致战力减半的情况。

---

## 10. 将补给从每月跳变改为滚动结算

### 当前问题

移动和战斗按日推进，但普通军队的补给、饥饿伤亡和相关士气惩罚可能主要按约 30 天结算。

这会造成：

- 月初断补给后，军队长时间几乎不受影响；
- 到结算日突然承受大量伤亡和士气下降；
- 玩家可以利用结算日期进行进攻；
- 战斗节奏与后勤节奏脱节。

### 修改要求

改为每日或滚动累计结算。

推荐：

```text
daily_supply_need
daily_supply_received
supply_deficit
accumulated_supply_debt
```

补给不足的影响可以逐日累积：

- 先降低恢复；
- 再降低移动速度；
- 再降低战斗效率；
- 最后造成士气和非战斗减员。

避免第一天断补给就遭受巨大惩罚，也避免 29 天完全无影响。

### 验收标准

- 断补给后的影响逐步出现，而不是固定日期突然跳变。
- 同样断补给 10 天，无论发生在月初还是月末，结果基本一致。
- 补给恢复后，惩罚按合理速度减轻。
- 保存和读档不会重置补给债务。

---

## 11. 明确定义三方及多方战斗规则

### 当前问题

同一路段出现第三方时，系统可能将多方战斗串行处理：

1. 两方先交战；
2. 第三方等待；
3. 第三方再攻击胜者。

如果交战顺序受距离、ID 或遍历顺序影响，那么内部处理顺序会变成战略优势。

### 修改要求

至少明确采用以下一种模型。

#### 方案 A：真正多方战斗

- 每个敌对阵营单独作为一侧；
- 每日选择目标或按威胁分配攻击；
- 同盟和敌对关系实时生效；
- 支持多方撤退和胜负判定。

#### 方案 B：显式串行战斗

如果暂不实现真正多方战斗，则必须：

- 使用明确、对称、可解释的优先级；
- 不允许由 ID 或遍历顺序决定；
- UI 明确显示谁在等待、谁先交战以及原因；
- 等待方不得无代价地坐收渔利；
- 可以加入等待期间的时间消耗、暴露风险或介入选择。

### 验收标准

- 改变军队 ID 不会改变多方交战顺序。
- 同一局面在固定种子下结果可复现。
- 玩家能够从状态或日志中理解交战顺序。
- 第三方不会因为内部遍历顺序获得无解释优势。

---

## 12. 防止军队拆分套利

### 当前问题

多个规则可能受到“军队对象数量”影响，而不是实际兵力影响，例如：

- 伤亡取整；
- 援军士气奖励；
- 预备队补入；
- 随机判定次数；
- 状态恢复；
- 围城贡献。

玩家可能通过将一支军队拆成多支获得额外收益。

### 修改要求

所有核心公式优先基于：

- 总有效兵力；
- frontage 占用；
- 实际新增兵力；
- 总伤亡；
- 总补给需求；

而不是军队对象数量。

需要对以下场景进行拆分等价性测试：

- 一支 10,000 人军队；
- 两支 5,000 人军队；
- 十支 1,000 人军队。

除组织、指挥、路线等明确设计差异外，三者的基础战斗结果应基本一致。

### 验收标准

- 拆分不会降低总伤亡。
- 拆分不会重复获得增援士气。
- 拆分不会增加总 frontage。
- 拆分不会额外增加围城效率。
- 拆分不会额外增加随机判定机会。

---

# 五、代码结构要求

## 13. 集中管理战斗参数

将以下参数集中到配置资源、常量文件或数据对象中：

```text
side_rout_threshold
army_rout_threshold
minimum_combat_efficiency
reinforcement_radius
frontage_capacity
reserve_reinforcement_rate
terrain_modifiers
shared_random_range
side_random_range
siege_ratio_curve
daily_supply_penalties
```

要求：

- 不要在多个函数中散落魔法数字；
- 参数应有单位和注释；
- 配置修改后不需要改核心算法；
- 测试可覆盖不同参数组合。

---

## 14. 分离计算与状态修改

尽量将以下逻辑提取为纯函数：

- 计算有效攻击力；
- 计算士气；
- 计算是否溃败；
- 分配伤亡；
- 计算增援 ETA；
- 计算 frontage；
- 计算围城效率；
- 计算补给惩罚；
- 判定多方交战顺序。

纯函数应：

- 不直接读取全局状态；
- 不直接修改军队对象；
- 输入完整；
- 输出可测试；
- 支持固定种子或注入随机源。

战斗状态机负责调用这些函数并应用结果。

---

## 15. 增加战斗日志

为调试和回放增加结构化日志，至少包含：

```text
battle_id
day
participants
frontline_strength
reserve_strength
effective_attack
effective_defense
shared_random_modifier
side_random_modifier
terrain_modifier
supply_modifier
casualties
morale_before
morale_after
reinforcements_arrived
rout_reason
winner_or_draw
```

要求：

- 日志可关闭；
- 测试环境可读取；
- 不依赖自然语言文本进行逻辑判断；
- 能用于定位战斗结果为何产生。

---

# 六、测试要求

## 16. 必须新增的单元测试

### 对称性测试

- 交换 A/B 方后结果等价。
- 改变军队 ID 后结果不变。
- 改变创建顺序后结果不变。

### 同时崩溃测试

- 双方同时士气归零。
- 双方同时兵力归零。
- 双方同一轮同时达到撤退阈值。
- 不得固定 A 方获胜。

### 伤亡守恒测试

- 少量伤亡分配给大量小部队。
- 总伤亡超过部分军队剩余兵力。
- 所有军队被消灭。
- 拆分前后总伤亡一致。

### 士气测试

- 零士气部队不满额输出。
- 极小援军不能复活整条战线。
- 多支援军与单支援军效果等价。
- 士气在有效范围内。

### 增援测试

- 同一路段远距离部队不能瞬间参战。
- ETA 到达后正常加入。
- 战斗提前结束后取消加入。
- 保存读档后 ETA 正确。

### 正面宽度测试

- 超额兵力进入预备队。
- 前线损失后预备队补入。
- 军队拆分不增加 frontage。
- 不同地形产生不同 frontage。

### 围城测试

- 有无驻军时量纲一致。
- 驻军被击败后仍由 fort strength 决定城防。
- 围城效率随兵力比连续变化。
- 不出现攻击、撤退无限循环。

### 地形测试

- 参数小幅变化不会产生巨大跳变。
- 明确 choke point 类型产生预期效果。
- 浮点边界稳定。

### 补给测试

- 同样断补给天数得到相近结果。
- 月初和月末断补给无明显差异。
- 恢复补给后惩罚逐步消退。

### 多方战斗测试

- 三方参战顺序不依赖 ID。
- 固定种子下结果可复现。
- 第三方状态和介入规则正确。

---

## 17. 必须新增的统计测试

除普通单元测试外，增加批量模拟测试。

建议至少运行：

```text
10,000 次对称战斗
```

验证：

- A 方胜率和 B 方胜率接近；
- 双方交换位置后统计结果一致；
- 优势方拥有稳定且合理的胜率；
- 随机性不会覆盖明显的兵力优势；
- 拆分军队不会显著改变胜率；
- 地形优势符合预期；
- 固定种子可完全复现。

统计测试允许设置合理误差区间，不要求每次完全相同。

---

# 七、推荐实施顺序

## 阶段 1：先修正确性

1. 修复同时崩溃和 A 方偏置；
2. 重构伤亡整数分配；
3. 让士气影响战斗效率和溃败；
4. 修复援军士气拆分套利；
5. 增加对称性和守恒测试。

## 阶段 2：恢复空间规则

1. 引入战场接触点；
2. 增加增援 ETA；
3. 增加 frontage 和预备队；
4. 更新移动、战斗和状态机；
5. 增加空间和增援测试。

## 阶段 3：重构围城

1. 拆分驻军、城防和城市属性；
2. 替换硬门槛为连续效率曲线；
3. 修复攻城后自动撤退循环；
4. 增加围城测试。

## 阶段 4：改善整体体验

1. 拆分共享随机和阵营独立随机；
2. 平滑地形修正；
3. 将补给改为滚动结算；
4. 明确三方和多方战斗规则；
5. 增加批量统计测试。

---

# 八、完成定义

仅修改代码但没有测试，不算完成。

任务完成必须同时满足：

- 所有现有测试通过，或已因规则变化进行合理更新；
- 新增测试覆盖本任务书中的关键场景；
- 战斗结果不依赖 A/B 方、军队 ID 或遍历顺序；
- 总伤亡严格守恒；
- 士气真实影响战斗能力；
- 远距离援军不能瞬间参战；
- 围城不存在量纲混用；
- 地形修正不存在无意的数值断崖；
- 补给不再依赖月度瞬间跳变；
- 多方战斗规则明确且可复现；
- 核心参数集中配置；
- 关键公式和变量单位有注释；
- 提供一份变更说明，列出：
  - 修改的文件；
  - 核心规则变化；
  - 新增测试；
  - 尚未处理的风险；
  - 可能影响存档兼容性的字段变化。

---

# 九、给 Agent 的执行要求

请先阅读项目中的战斗、移动、围城、补给、军队模型和测试代码，再开始修改。

执行时遵守以下要求：

1. 不要只针对单个测试打补丁；
2. 不要通过固定 A/B 顺序规避问题；
3. 不要引入新的基于军队 ID 的判定；
4. 不要将新的魔法数字散落在逻辑中；
5. 优先提取可独立测试的纯函数；
6. 保持现有存档兼容性；若无法保持，必须提供迁移或默认值；
7. 每完成一个阶段运行全部测试；
8. 最终输出：
   - 变更摘要；
   - 修改文件列表；
   - 新增和更新的测试；
   - 测试运行结果；
   - 尚未完成的事项；
   - 需要人工确认的设计取舍。
